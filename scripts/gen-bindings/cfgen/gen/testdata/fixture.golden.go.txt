// Code generated by cfgen from @cloudflare/workers-types@0.0.0-fixture. DO NOT EDIT.

//go:build js && wasm

// Package widgets is a synthetic fixture package used only by cfgen's
// golden test. It exercises: a handle type, a data type, a string-literal
// alias enum, a Promise-returning method, a Record-returning method, an
// array field, an optional field, a rename, a types override, and a
// handwritten method.
package widgets

import (
	"syscall/js"

	"github.com/syumai/workers-go/exp/internal/jsrt"
)

// Widget Widget is a handle type.
type Widget struct{ v js.Value }

// WidgetFromJS wraps a JS Widget value.
func WidgetFromJS(v js.Value) *Widget { return &Widget{v: v} }

// JSValue returns the underlying JS value.
func (x *Widget) JSValue() js.Value { return x.v }

// NewWidget resolves the binding named bindingName from env.
func NewWidget(bindingName string) (*Widget, error) {
	v, err := jsrt.Binding(bindingName)
	if err != nil {
		return nil, err
	}
	return &Widget{v: v}, nil
}

// ID
func (x *Widget) ID() string {
	var ret string
	ret = x.v.Get("id").String()
	return ret
}

// Get get fetches a WidgetInfo by key.
func (x *Widget) Get(key string) (WidgetInfo, error) {
	p, err := jsrt.Call(x.v, "get", key)
	if err != nil {
		return WidgetInfo{}, err
	}
	r, err := jsrt.Await(p)
	if err != nil {
		return WidgetInfo{}, err
	}
	var ret WidgetInfo
	if tmp, err := widgetInfoFromJS(r); err != nil {
		return WidgetInfo{}, err
	} else {
		ret = tmp
	}
	return ret, nil
}

// Counters counters returns a map of named counters.
func (x *Widget) Counters() (map[string]float64, error) {
	r, err := jsrt.Call(x.v, "counters")
	if err != nil {
		return nil, err
	}
	var ret map[string]float64
	ret = make(map[string]float64)
	keys := js.Global().Get("Object").Call("keys", r)
	for i := 0; i < keys.Length(); i++ {
		k := keys.Index(i).String()
		var vv float64
		vv = r.Get(k).Float()
		ret[k] = vv
	}
	return ret, nil
}

// WidgetInfo WidgetInfo is a data type.
type WidgetInfo struct {
	// the widget's display name
	Name     string       `js:"widget_name"`
	CacheTTL *int         `js:"cacheTtl"`
	Tags     []string     `js:"tags"`
	Status   WidgetStatus `js:"status"`
	// raw escape hatch, not translated
	Secret js.Value `js:"secret"`
}

func widgetInfoFromJS(v js.Value) (WidgetInfo, error) {
	var out WidgetInfo
	{
		out.Name = v.Get("widget_name").String()
	}
	{
		if s := v.Get("cacheTtl"); !s.IsUndefined() && !s.IsNull() {
			n := s.Int()
			out.CacheTTL = &n
		}
	}
	{
		out.Tags = make([]string, v.Get("tags").Length())
		for i := range out.Tags {
			out.Tags[i] = v.Get("tags").Index(i).String()
		}
	}
	{
		out.Status = WidgetStatus(v.Get("status").String())
	}
	{
		out.Secret = v.Get("secret")
	}
	return out, nil
}

func (o WidgetInfo) toJS() js.Value {
	obj := jsrt.NewObject()
	if o.Name != "" {
		obj.Set("widget_name", o.Name)
	}
	if o.CacheTTL != nil {
		obj.Set("cacheTtl", *o.CacheTTL)
	}
	if len(o.Tags) > 0 {
		arr := js.Global().Get("Array").New(len(o.Tags))
		for i, e := range o.Tags {
			arr.SetIndex(i, e)
		}
		obj.Set("tags", arr)
	}
	if o.Status != "" {
		obj.Set("status", string(o.Status))
	}
	if !jsrt.IsNil(o.Secret) {
		obj.Set("secret", o.Secret)
	}
	return obj
}

// WidgetStatus WidgetStatus is a string-literal union alias.
type WidgetStatus string

const (
	WidgetStatusActive   WidgetStatus = "active"
	WidgetStatusInactive WidgetStatus = "inactive"
)

// WidgetFactory WidgetFactory.create is handwritten.
type WidgetFactory struct{ v js.Value }

// WidgetFactoryFromJS wraps a JS WidgetFactory value.
func WidgetFactoryFromJS(v js.Value) *WidgetFactory { return &WidgetFactory{v: v} }

// JSValue returns the underlying JS value.
func (x *WidgetFactory) JSValue() js.Value { return x.v }
