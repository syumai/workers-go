{
  "name": "<TBD>",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "go run github.com/syumai/workers-go/cmd/workers-assets-gen && tinygo build -o ./build/app.wasm -target wasm -no-debug ./...",
    "deploy": "wrangler pages deploy ./pages",
    "dev": "wrangler pages dev ./pages",
    "start": "wrangler pages dev ./pages"
  },
  "devDependencies": {
    "wrangler": "^4.9.1"
  }
}
