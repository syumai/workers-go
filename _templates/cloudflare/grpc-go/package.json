{
  "name": "<TBD>",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "go run github.com/syumai/workers-go/cmd/workers-assets-gen -mode=go && GOOS=js GOARCH=wasm go build -ldflags=\"-s -w\" -o ./build/app.wasm .",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --compatibility-flags experimental",
    "start": "wrangler dev --compatibility-flags experimental",
    "generate": "buf generate"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
